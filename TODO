add MouseWheelUp and MouseWheelDown
add simple pager model
add sdl driver
add paths package with dijkstra, astar
add game events queue package ?
add animation utilities package ?
add tiles package with utilities for drawing fonts
add map generation utilities package ?
compress replay frames?
send MsgScreenSize at start and on resize (when sensible) [half done]
add horizontal menu option, configuration for box color
