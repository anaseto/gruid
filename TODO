----- TODO
add horizontal menu option/status bar
add more tests
add animation utilities package ? synchronize with renderer?
add game events queue package ?
add map generation utilities package ?
----- Partial
send MsgScreenSize at start and on resize (when sensible) [done for tcell]
